<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Ticket Booking</title>
</head>
<body>
  <h1>Edit Ticket Booking</h1>
  <form action="/customer/tickets/<%= ticket._id %>?_method=PUT" method="POST">
    <label for="trainId">Train:</label>
    <input type="text" value="<%= ticket.trainId.name %> (Train No: <%= ticket.trainId.trainNumber %>)" disabled>

    <label for="seatCount">Number of Seats:</label>
    <input type="number" name="seatCount" id="seatCount" min="1" value="<%= ticket.seatCount %>" required>

    <button type="submit">Update Booking</button>
  </form>
  <a href="/customer/tickets">Back to My Tickets</a>
</body>
</html>
